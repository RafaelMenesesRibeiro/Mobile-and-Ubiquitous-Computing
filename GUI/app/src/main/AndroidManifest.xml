<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="cmov1819.p2photo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_INTERNAL_STORAGE" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning"
        tools:targetApi="m">

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.suggestedIntent.action.MAIN" />
                <category android:name="android.suggestedIntent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AuthenticationActivity"/>

        <activity
            android:name="net.openid.appauth.RedirectUriReceiverActivity"
            tools:node="replace">
            <intent-filter>
                <action android:name="android.suggestedIntent.action.VIEW" />
                <category android:name="android.suggestedIntent.category.DEFAULT" />
                <category android:name="android.suggestedIntent.category.BROWSABLE" />
                <data android:scheme="cmov1819.p2photo" />
            </intent-filter>
        </activity>

        <activity android:name=".MainMenuActivity" />
        <activity android:name=".NewAlbumMemberActivity" />
        <activity android:name=".NewAlbumActivity" />
        <activity android:name=".SearchUserActivity" />
        <activity android:name=".ShowUserAlbumsActivity" />
        <activity android:name=".ShowAlbumActivity" />
        <activity android:name=".AddPhotosActivity" />
        
    </application>

</manifest>